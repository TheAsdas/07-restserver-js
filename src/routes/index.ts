import auth_router from "./auth";
import category_router from "./categories";
import user_router from "./users";

export const auth = auth_router;
export const categories = category_router;
export const users = user_router;
